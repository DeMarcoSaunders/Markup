# Markup v2 library

This folder is the **MarkUp v2** library: public API in `include/markup/`, implementation in `src/`.

**Setup, build commands, getting started, and how to add new widget kinds** are documented in the [repository root README](../README.md).
